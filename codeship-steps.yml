- type: parallel
  services:
  - builder
  - unittests
  steps:
  - command: echo Build complete

- type: parallel
  services:
  - deploysplunk
  steps:
  - command: echo Deployed Splunk!

- type: parallel
  services:
  - integrationtests
  steps:
  - command: echo Deployed Splunk!
