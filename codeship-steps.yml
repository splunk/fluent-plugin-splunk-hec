- type: serial
  service: builder
  steps:
  - command: echo Build complete

- type: serial
  service: unittests
  steps:
  - command: echo Unit Tests complete!
  - command: gem build fluent-plugin-splunk-hec.gemspec

- type: parallel
  services: 
  - deploysplunk
  - fluentd
  steps:
  - command: echo Deployed Splunk!
  depends_on: unittests
