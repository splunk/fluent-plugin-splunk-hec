- type: serial
  services:
  - builder
  steps:
  - command: echo Build complete

# Run unit tests against different rvm versions
- type: parallel
  services:
  - unittests
  steps:
  - command: rake test